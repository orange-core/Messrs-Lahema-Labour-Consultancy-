# Add these 2 images here

**lahema-logo.png** — the Lahema logo (from your zip, already correct)
**orange-core-logo.png** — the Orange Core logo (from your zip, already correct)

Drag both into this folder on GitHub (web UI, "Add file" → "Upload files"). Everything else is already wired up to these exact filenames — nothing else to change.
